import Body from './Body';
import Footer from './Footer';
import AppContainer from '../common/AppContainer';

const Home = () => {
  return (
    <AppContainer>
      <Body />
      <Footer />
    </AppContainer>
  );
};

export default Home;
