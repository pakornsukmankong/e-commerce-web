import AppContainer from '../common/AppContainer';
import LoginForm from './LoginForm';

const Login = () => {
  return (
    <AppContainer>
      <LoginForm />
    </AppContainer>
  );
};

export default Login;
