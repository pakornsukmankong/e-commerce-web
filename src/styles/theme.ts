import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
    fonts: {
        heading: `Noto Sans Thai, Inter, Arial, sans-serif`,
        body: `Noto Sans Thai, Inter, Arial, sans-serif`
    }
})

export default theme